import { ClientData } from "./blocks/ClientData.js";
import { ClientReservations } from "./blocks/ClientReservations.js";

const SectionContent =
        `${ClientData}
        ${ClientReservations}`;

export const ViewClientSection = SectionContent;